'use strict';

/**
 * @ngdoc function
 * @name slangoApp.controller:SlangCtrl
 * @description
 * # SlangCtrl
 * Controller of the slangoApp
 */
angular.module('slangoApp')
  .controller('SlangCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
